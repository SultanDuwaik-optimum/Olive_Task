export interface Address{
    city: string,
    neighborhood: string,
    street: string
}

export interface CountryInfo{
    flag: string;
    name: string;
    keyCode: number;
    countryFormat: string;
}